package com.pruebatecnica.servicioavlachile.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import com.pruebatecnica.servicioavlachile.entity.PhoneEntity;

// @Repository
// public interface PhoneRepository extends JpaRepository<PhoneModel, Long>{
//     PhoneModel findByPhoneNumber(Number phoneNumber);
// }
